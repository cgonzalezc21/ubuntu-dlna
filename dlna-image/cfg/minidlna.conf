# Configuracion general del directorio media (volumen media). 
# Se especifica la "V" al inicio para indicar que los medios son videos
media_dir=V,/var/lib/minidlna

# Se añade un nombre amigable al servidor
friendly_name=ubuntu_dlna

# Se habilita la detección automática de contenidos en el volumen
inotify=yes

# Se configura el intervalo de notificación SSDP, en segundos
notify_interval=60

# Puerto de escucha
port=8200
